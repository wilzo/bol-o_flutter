name: app_novo_mobile
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  cloud_firestore: ^5.5.1
  cupertino_icons: ^1.0.6
  firebase_auth: ^5.3.4
  firebase_core: ^3.8.1
  firebase_storage: ^12.4.2
  flutter:
    sdk: flutter
  flutter_map: ^7.0.2
  geolocator: ^13.0.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image_picker: ^1.1.2
  intl: ^0.20.2
  latlong2: ^0.9.1
  path_provider: ^2.1.5
  provider: ^6.1.2
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # Adicione esta seção para incluir os assets (imagens, fontes, etc.)
  assets:
    - assets/if_logo.png # Caminho para a logo do IF